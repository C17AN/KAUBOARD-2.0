import React from "react";

type Props = {};

const Sidebar = (props: Props) => {
  return <aside className="w-64 border-r-4 border-r-slate-500 p-8 dark:bg-black">프로필</aside>;
};

export default Sidebar;
